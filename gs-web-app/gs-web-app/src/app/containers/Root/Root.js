import React from 'react';

import styles from './Root.module.scss';

const Root = (props) => {
    return (
        <div>
            <h2 className={styles.Text}>Test</h2>
        </div>
    );
};

export default Root;

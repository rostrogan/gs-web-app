const ROUTE_USERS = '/api/users';
const ROUTE_REGISTER = '/api/register';
const ROUTE_LOGIN = '/api/login';

const COMMON_ROOT = '/';

module.exports = {
    COMMON_ROOT,
    ROUTE_USERS,
    ROUTE_REGISTER,
    ROUTE_LOGIN
};

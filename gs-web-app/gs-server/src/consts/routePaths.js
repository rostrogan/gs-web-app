const COMMON_ROOT = '/';

const ROUTE_USERS = '/api/users';
const ROUTE_FACULTIES = '/api/faculties';
const ROUTE_GROUPS = '/api/groups';

module.exports = {
    COMMON_ROOT,
    ROUTE_FACULTIES,
    ROUTE_GROUPS,
    ROUTE_USERS,
};
